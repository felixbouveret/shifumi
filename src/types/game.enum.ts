export enum UserType {
  PLAYER = "player",
  OPPONENT = "opponent",
}

export enum CardType {
  ROCK = "rock",
  PAPER = "paper",
  SCISSORS = "scissors",
  UNKNOWN = "unknown",
}

export enum BoardParts {
  HAND = "playerHand",
  BOARD = "playerBoard",
}
